---
scope: Enhanced Deja Brew redesign - 6 CAPs, 7 stories
purpose: Invariants keeping warm editorial, story-fused, unified menu, cart demo from diverging
altitude: feature
updated: 2026-08-27T02:18
---

- (decision) AD-1 Paradigm: Static-site + Progressive Enhancement - HTML is source of truth, JS only enhances (tabs/cart/progress), never required for core browse/order link
- (decision) AD-2 Dependency direction: design-system -> pages -> interactions; pages may depend on catalog, catalog never depends on pages; cart depends on catalog + localStorage, nothing depends on cart
- (decision) AD-3 State ownership: catalog is read-only JSON derived from menu-catalog.md rendered as data/js variable; cart state owned solely by cart module via localStorage key deja-brew-cart, no second writer
- (decision) AD-4 Routing invariant: single Main.html + single MenuOrder page + order-success.html; no new routes without AD amendment; nav hrefs stay sibling HTML, About is anchor #about
- (decision) AD-5 Styling invariant: no framework, single style.css + design-system tokens; JS never injects styles except progress width and cart drawer transform; tokens are source of truth
- (version) Stack: HTML5 + CSS3 + Vanilla JS (ES2020) + LocalStorage, static host (GitHub Pages/Netlify/static), Font Awesome 6 CDN, no build step
- (event) spine finalized - 5 ADs, paradigms static-site progressive enhancement, deferred 4 items
